

export const selectFilterByName = state => state.filter;

export const selectTrack = state => state.track.items;

export const selectIsLoading = state => state.track.isLoading;

export const selectError = state => state.track.error;
